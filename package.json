{
  "name": "fetch-challenge",
  "version": "1.0.0",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run --browser=chrome --headless"
  },
  "devDependencies": {
    "cypress": "^13.13.0"
  }
}
